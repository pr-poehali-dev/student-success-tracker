# 🚀 Инструкция по развёртыванию на собственном сервере Ubuntu

Эта инструкция поможет вам перенести приложение Student Success Tracker с poehali.dev на ваш собственный Ubuntu сервер.

## 📋 Что вы получите

После выполнения всех шагов у вас будет:
- ✅ **Фронтенд** - React SPA приложение (статические файлы)
- ✅ **Backend** - FastAPI сервер с REST API
- ✅ **База данных** - PostgreSQL со всеми данными
- ✅ **Nginx** - веб-сервер для раздачи фронтенда и проксирования API
- ✅ **Docker Compose** - всё запускается одной командой

---

## 🎯 Шаг 1: Экспорт данных из базы данных

Перед переносом нужно выгрузить данные из текущей БД poehali.dev.

### 1.1. Получите строку подключения к БД

Откройте в браузере (ссылка будет активна пока не попросите удалить функцию):
```
https://functions.poehali.dev/a6f07eb2-4c19-423a-a564-fe3d37a9c904
```

Вы увидите JSON с полем `database_url` - это строка подключения к БД. Скопируйте её.

### 1.2. Установите PostgreSQL клиент (если ещё не установлен)

На вашем локальном компьютере (Windows/Mac/Linux):

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install postgresql-client python3-psycopg2
```

**macOS:**
```bash
brew install postgresql
```

**Windows:**
Скачайте с https://www.postgresql.org/download/windows/

### 1.3. Запустите скрипт экспорта данных

```bash
# Установите psycopg2 если нужно
pip install psycopg2-binary

# Откройте файл server/export_data.py
# Замените строку DATABASE_URL на скопированную из браузера
# Запустите скрипт
python server/export_data.py
```

Скрипт создаст файл `data_dump.sql` с данными из вашей БД.

---

## 🎯 Шаг 2: Подготовка сервера

### 2.1. Подключитесь к серверу Ubuntu

```bash
ssh user@your-server-ip
```

### 2.2. Установите Docker и Docker Compose

```bash
# Обновите систему
sudo apt-get update
sudo apt-get upgrade -y

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиньтесь для применения изменений
exit
ssh user@your-server-ip

# Проверьте установку
docker --version
docker compose version
```

---

## 🎯 Шаг 3: Загрузка проекта на сервер

### 3.1. Соберите билд фронтенда локально

На вашем компьютере в папке проекта:

```bash
# Установите зависимости (если ещё не установлены)
npm install

# Создайте файл с настройками для продакшена
cp .env.production .env

# Отредактируйте .env и укажите IP вашего сервера
# VITE_API_URL=http://YOUR_SERVER_IP/api/sync
nano .env

# Соберите билд
npm run build
```

Это создаст папку `dist/` с готовым фронтендом.

### 3.2. Скопируйте файлы на сервер

```bash
# Создайте папку проекта на сервере
ssh user@your-server-ip "mkdir -p ~/student-tracker"

# Скопируйте все необходимые файлы
scp -r dist/ user@your-server-ip:~/student-tracker/
scp -r server/ user@your-server-ip:~/student-tracker/
scp data_dump.sql user@your-server-ip:~/student-tracker/
scp docker-compose.yml user@your-server-ip:~/student-tracker/
scp nginx.conf user@your-server-ip:~/student-tracker/
scp .env.example user@your-server-ip:~/student-tracker/
```

---

## 🎯 Шаг 4: Настройка и запуск на сервере

### 4.1. Подключитесь к серверу и перейдите в папку проекта

```bash
ssh user@your-server-ip
cd ~/student-tracker
```

### 4.2. Создайте файл с паролями

```bash
cp .env.example .env
nano .env
```

Установите надёжный пароль для PostgreSQL:
```
DB_PASSWORD=ваш_надёжный_пароль_123
DB_SCHEMA=public
```

Сохраните (Ctrl+O, Enter, Ctrl+X).

### 4.3. Запустите всё приложение

```bash
# Запуск всех контейнеров (PostgreSQL + Backend + Nginx)
docker compose up -d

# Проверьте статус
docker compose ps

# Посмотрите логи (если нужно)
docker compose logs -f
```

### 4.4. Проверьте работу

Откройте в браузере:
```
http://YOUR_SERVER_IP
```

Вы должны увидеть ваше приложение с данными из БД!

---

## 🎯 Шаг 5: Настройка домена (опционально)

### 5.1. Настройте DNS записи

В панели вашего регистратора доменов добавьте A-запись:
```
Type: A
Name: @  (или ваш поддомен)
Value: YOUR_SERVER_IP
TTL: 3600
```

### 5.2. Установите SSL сертификат (HTTPS)

```bash
# Установите Certbot
sudo apt-get install certbot python3-certbot-nginx

# Остановите контейнер nginx временно
docker compose stop nginx

# Получите сертификат
sudo certbot certonly --standalone -d yourdomain.com

# Обновите nginx.conf для работы с SSL
```

Добавьте в `nginx.conf` секцию для HTTPS:
```nginx
server {
    listen 443 ssl;
    server_name yourdomain.com;
    
    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
    
    # ... остальные настройки как в server { listen 80; }
}

server {
    listen 80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}
```

Добавьте volumes в `docker-compose.yml` для nginx:
```yaml
nginx:
  volumes:
    - ./dist:/usr/share/nginx/html
    - ./nginx.conf:/etc/nginx/nginx.conf
    - /etc/letsencrypt:/etc/letsencrypt:ro
```

Перезапустите:
```bash
docker compose up -d
```

---

## 🛠️ Управление приложением

### Остановка
```bash
docker compose stop
```

### Запуск
```bash
docker compose start
```

### Перезапуск
```bash
docker compose restart
```

### Полная остановка с удалением контейнеров
```bash
docker compose down
```

### Просмотр логов
```bash
# Все сервисы
docker compose logs -f

# Только backend
docker compose logs -f backend

# Только база данных
docker compose logs -f db
```

### Резервное копирование БД
```bash
docker compose exec db pg_dump -U postgres student_tracker > backup_$(date +%Y%m%d).sql
```

### Восстановление из бэкапа
```bash
docker compose exec -T db psql -U postgres student_tracker < backup_20240101.sql
```

---

## 🔒 Безопасность

### Важные рекомендации:

1. **Измените пароль БД** в файле `.env` на надёжный
2. **Настройте firewall**:
   ```bash
   sudo ufw allow 22/tcp    # SSH
   sudo ufw allow 80/tcp    # HTTP
   sudo ufw allow 443/tcp   # HTTPS
   sudo ufw enable
   ```
3. **Регулярно обновляйте систему**:
   ```bash
   sudo apt-get update && sudo apt-get upgrade -y
   ```
4. **Настройте автоматические бэкапы БД** (cron)
5. **Используйте HTTPS** с Let's Encrypt

---

## ❓ Решение проблем

### Приложение не запускается

```bash
# Проверьте статус контейнеров
docker compose ps

# Посмотрите логи
docker compose logs -f
```

### База данных не подключается

```bash
# Проверьте здоровье БД
docker compose exec db pg_isready -U postgres

# Подключитесь к БД вручную
docker compose exec db psql -U postgres -d student_tracker
```

### Backend возвращает ошибки

```bash
# Проверьте логи backend
docker compose logs backend

# Перезапустите backend
docker compose restart backend
```

### Фронтенд не загружается

```bash
# Проверьте nginx логи
docker compose logs nginx

# Убедитесь что dist/ папка существует
ls -la dist/
```

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи всех сервисов: `docker compose logs -f`
2. Убедитесь что все порты открыты в firewall
3. Проверьте что `data_dump.sql` существует перед первым запуском
4. Убедитесь что в `.env` указан правильный пароль

---

## ✅ Чек-лист перед запуском

- [ ] Экспортировал данные из БД poehali.dev (data_dump.sql создан)
- [ ] Установил Docker и Docker Compose на сервере
- [ ] Собрал билд фронтенда (npm run build)
- [ ] Скопировал все файлы на сервер
- [ ] Создал файл .env с надёжным паролем
- [ ] Запустил docker compose up -d
- [ ] Открыл приложение в браузере и проверил работу
- [ ] Настроил домен (опционально)
- [ ] Установил SSL сертификат (опционально)
- [ ] Настроил автоматические бэкапы БД

---

## 🎉 Готово!

Теперь ваше приложение работает на собственном сервере полностью автономно, без зависимости от poehali.dev!

Все запросы к API идут на ваш сервер, данные хранятся в вашей БД.
